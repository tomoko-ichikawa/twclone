<h1>つぶやき</h1>

<table>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.content %></td>
    <td><%= link_to '詳細を確認する', blog_path(blog.id) %></td>
    <td><%= link_to "つぶやきを編集する", edit_blog_path(blog.id) %></td>
  </tr>
<% end %>
</table>
<br>
<td><%= link_to 'つぶやきを投稿する',new_blog_path %></td>
